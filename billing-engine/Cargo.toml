[package]
name = "billing-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
rust_decimal = { version = "1", features = ["macros"] }
rust_decimal_macros = "1"
thiserror = "2"

[dev-dependencies]
proptest = "1"
